
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.beonse2.domain.coupon.mapper.CouponMapper">

    <insert id="saveCoupon" parameterType="com.beonse2.coupon.vo.CouponVO">
        INSERT INTO mycoupon (cid, branch_bid, type, price)
        VALUES (mycoupon_seq.NEXTVAL, 1, #{type}, #{price})
    </insert>

    <select id="findAllByMemberIdAndCouponIdOrderByPaymentDateDesc" resultType="List<com.beonse2.domain.coupon.vo.MyCouponVO>" parameterType="int">
        SELECT mcpid, member_mid, branch_bid, type, price, is_used, payment_date
        FROM mycoupon
        WHERE mcpid = 1
    </select>


</mapper>