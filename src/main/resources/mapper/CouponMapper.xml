
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.beonse2.domain.coupon.mapper.CouponMapper">

    <insert id="saveCoupon" parameterType="com.beonse2.coupon.point.vo.CouponVO">
        INSERT INTO coupon (cid, member_mid, branch_bid, type, price, is_used, payment_date)
        VALUES (coupon_seq.NEXTVAL, 1, 1, #{type}, #{price}, #{is_used}, SYSTIMESTAMP)
    </insert>

    <select id="findById" resultType="com.beonse2.domain.coupon.vo.CouponVO" parameterType="int">
        SELECT cid, member_mid, branch_bid, type, price, is_used, payment_date
        FROM coupon
        WHERE cid = 1
    </select>
</mapper>